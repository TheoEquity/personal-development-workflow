# 依赖说明

本仓库发布五个自有 Skill，运行时不强制依赖完整的 Superpowers 流程链。

Codex 或其他宿主应提供 Git、Python 3 和基本文件编辑/命令执行能力。创建 Worktree 时优先使用宿主的原生能力；没有原生能力时可以使用 Git `worktree` 命令。

以下能力按任务需要使用，不是安装门禁：

- 计划：宿主内置 Plan 能力或任意轻量计划方式；
- 并行：宿主的子 Agent 能力，仅用于真正独立的工作流；
- 验证：项目自身测试、构建、lint 和人工可复核证据；
- 分支收尾：项目已有的 Git/MR/PR 流程。

项目特有的不变量放在仓库的 `AGENTS.md`、测试、CI 或 Hook 中，不复制进全局工作流 Skill。
